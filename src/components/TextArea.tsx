import styled from "@emotion/styled";
import { TextArea as BaseTextArea } from "@radix-ui/themes";

export const TextArea = styled(BaseTextArea)`
  width: 100%;
  height: 100%;
`;

export default TextArea;
